services:
  backend:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    working_dir: /var/www/backend
    command: sh -c "if [ ! -f vendor/autoload.php ]; then composer install --no-interaction; fi; php-fpm"
    volumes:
      - ./backend:/var/www/backend
    environment:
      APP_ENV: local
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: discounts
      DB_USERNAME: discounts
      DB_PASSWORD: discounts
      REDIS_HOST: redis
      QUEUE_CONNECTION: redis
    depends_on:
      - postgres
      - redis

  queue:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    working_dir: /var/www/backend
    command: sh -c "until [ -f vendor/autoload.php ]; do sleep 2; done; php artisan queue:work --sleep=2 --tries=3 --timeout=180"
    volumes:
      - ./backend:/var/www/backend
    depends_on:
      - backend
      - redis
      - postgres

  scheduler:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    working_dir: /var/www/backend
    command: sh -c "until [ -f vendor/autoload.php ]; do sleep 2; done; php artisan schedule:work"
    volumes:
      - ./backend:/var/www/backend
    depends_on:
      - backend
      - redis
      - postgres

  nginx:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    volumes:
      - ./backend:/var/www/backend
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

  frontend:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run build && node .output/server/index.mjs"
    volumes:
      - ./frontend:/app
    environment:
      NUXT_PUBLIC_API_BASE: /api
      PORT: 3000
    depends_on:
      - nginx

  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: discounts
      POSTGRES_USER: discounts
      POSTGRES_PASSWORD: discounts
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

volumes:
  pgdata:
